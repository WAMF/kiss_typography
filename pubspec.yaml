name: kiss_typography
description: "A simple, clean Flutter typography package that provides semantic text widgets following the KISS (Keep It Simple, Stupid) principle."
version: 0.8.0
homepage: https://github.com/WAMF/kiss_typography

environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
